pipeline {
    agent any

    environment {
        // Configuración de SonarQube
        SONAR_HOST_URL = 'http://sonarqube:9000'
        SONAR_PROJECT_KEY = 'pygoat-analysis'
        SONAR_PROJECT_NAME = 'PyGoat'
    }

    stages {
        stage('Declarative: Checkout SCM') {
            steps {
                checkout scm
            }
        }

        stage('Checkout') {
            steps {
                echo "📥 Clonando el repositorio desde GitHub..."
                git branch: 'master', url: 'https://github.com/franpar410/pygoat.git'
            }
        }

        stage('SonarQube analysis') {
            steps {
                script {
                    echo "🔍 Iniciando análisis estático con SonarQube..."
                    withSonarQubeEnv('Sonarqube-docker') {
                        sh """
                        /var/jenkins_home/tools/hudson.plugins.sonar.SonarRunnerInstallation/sonarqube-scanner/bin/sonar-scanner \
                            -Dsonar.projectKey=${SONAR_PROJECT_KEY} \
                            -Dsonar.projectName=${SONAR_PROJECT_NAME} \
                            -Dsonar.sources=. \
                            -Dsonar.language=py \
                            -Dsonar.python.version=3.10 \
                            -Dsonar.host.url=${SONAR_HOST_URL}
                        """
                    }
                }
            }
        }

        stage('Quality Gate') {
            steps {
                timeout(time: 3, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }

        stage('Setup Python environment') {
            steps {
                echo "🐍 Configurando entorno Python en el contenedor Jenkins..."
                sh """
                    python3 -m venv venv
                    source venv/bin/activate
                    pip install --upgrade pip
                    pip install -r requirements.txt
                """
            }
        }

        stage('Tests & Security Check') {
            steps {
                echo "🧪 Ejecutando pruebas unitarias y análisis de seguridad..."
                sh """
                    source venv/bin/activate
                    echo "Ejecutando Bandit..."
                    bandit -r pygoat || true
                """
            }
        }

        stage('Build') {
            steps {
                echo "⚙️ Compilando proyecto Django (PyGoat)..."
                sh """
                    source venv/bin/activate
                    python manage.py check
                """
            }
        }

        stage('Deploy') {
            when {
                expression { return false } // Se omite por ahora hasta arreglar Build
            }
            steps {
                echo "🚀 Deploy stage skipped"
            }
        }
    }

    post {
        success {
            echo "✅ Pipeline finalizó correctamente"
        }
        failure {
            echo "❌ Pipeline falló. Revisar reporte de SonarQube en Jenkins o en ${SONAR_HOST_URL}"
        }
    }
}
