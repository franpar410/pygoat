pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                sh 'ls -l'
               // sh './build.sh'   ---Linea comentada
            }
        }
    }
}
