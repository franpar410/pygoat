pipeline {
    agent any

        stage('SCA-Safety') {
            agent {
                docker {
                    image 'python:3.11-slim'
                    args '-u root'
                }
            }
            steps {
                script {
                    echo "🔍 Ejecutando análisis de dependencias con Safety..."
        
                    sh '''
                        set -e
                        apt-get update -qq
                        apt-get install -y --no-install-recommends build-essential libpq-dev
                        pip install --disable-pip-version-check -q safety psycopg2-binary
                        if [ -f requirements.txt ]; then pip install --disable-pip-version-check -q -r requirements.txt; fi
        
                        # Ejecutar Safety y generar el reporte
                        mkdir -p /tmp/reports
                        safety check \
                          --full-report \
                          --json \
                          --output=/tmp/reports/safety.json \
                          --file=requirements.txt || true
        
                        # Copiar el reporte al workspace del host
                        cp /tmp/reports/safety.json "$WORKSPACE/safety.json" || echo "⚠️ No se generó safety.json"
                        ls -l "$WORKSPACE"
                    '''
        
                    // Evaluar resultados (bloquear o no)
                    def result = sh(script: "safety check --file=requirements.txt --exit-code", returnStatus: true)
                    if (result != 0) {
                        unstable(message: "⚠️ Vulnerabilidades detectadas por Safety")
                    }
                }
            }
        }


        stage('Compilation') {
            agent { docker { image 'php:8.2-cli' } }
            steps {
                sh 'echo "Compilando..."'
            }
        }

        stage('Build') {
            agent { docker { image 'php:8.2-cli' } }
            steps {
                sh 'echo "docker build -t my-php-app ."'
            }
        }

        stage('Deploy') {
            agent { docker { image 'php:8.2-cli' } }
            steps {
                sh 'echo "docker run my-php-app ."'
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: 'safety.json', fingerprint: true
        }
    }
}
